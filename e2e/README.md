# E2E (Playwright)

Runs against a live local stack (`make db-up && make dev`).

The suite grows with the spec's edge-case encyclopedia — every test is named
after its EC-ID from `docs/openlane_spec.md` §22 (e.g. `EC-PORTAL-07.spec.ts`).
